#include<stdio.h>

typedef int izuzu;  //typedef is used to give an alias to an existing datatype. can we give alias to an alias?

typedef izuzu isafuckboy;   //yes we can

int main(){
    isafuckboy a= 69;
    printf("%d\n",a);
    return 0;
}
